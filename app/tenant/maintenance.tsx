import React from "react";
import { View, Text } from "react-native";

export default function MaintenanceRequests() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Maintenance Requests (coming soon)</Text>
    </View>
  );
}
